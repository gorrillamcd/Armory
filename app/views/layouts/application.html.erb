<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Armory" %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <% render 'layouts/shim' %>
</head>
<body>

<!-- Navigation -->
<!--<header class="navbar navbar-fixed-top">
	<div class="navbar-inner">
        <div class="container">
          <%= link_to "IBCC en Linea", root_url, :id => "logo" %>
          <nav>
            <ul class="nav pull-right">
              <li><%= link_to "Home", root_url %></li>
              <li><%= link_to "Courses", courses_path %></li>
              <% if user_signed_in? %><li><%= link_to "User Management", users_path %></li><% end %>
              <li class="divider-vertical"></li>
                <% if user_signed_in? %>
                <li class="dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                      Signed in as <strong><%= current_user.email %></strong>
                      <b class="caret"></b>
                    </a>
                  <ul class="dropdown-menu">
                      <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
                  </ul>
                </li>
                <% else %>
                <li class="dropdown">
                  <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                      Account
                      <b class="caret"></b>
                    </a>
                  <ul class="dropdown-menu">
                      <li><%= link_to "Sign Up", new_user_registration_path %></li>
                      <li><%= link_to "Login", new_user_session_path %></li>
                  </ul>
                </li>
                <% end %>
              </li>
            </ul>
          </nav>
        </div>
      </div>
</header> -->
<!-- Body of Site -->
<div class="container">
  <!-- Show notices if any -->
    <div class="row">
    	<div class="span4 offset4">
	    		<% flash.each do |name, msg| %>
            <div id="flash_<%= name %>" class="alert alert-<%= name == :notice ? "success" : name %>">
                <%= msg %>
              <a class="close" data-dismiss="alert">x</a>
            </div>
				  <% end %>
		  </div>
    </div>
  <!-- END Notices Area -->

  <!-- Main Content Area -->
    <div class="row">
    <%# Super gross code here. Need to clean it up later %>
      <% unless @show_sidebar == false %><div class="span8"><% end %>
  			<%= yield %>
      <% unless @show_sidebar == false %></div><% end %>
  <!-- END Main Content Area -->

      <%# only show the sidebar if the @show_sidebar variable is true %>
      <% unless @show_sidebar == false %>
      <!-- Right Sidebar Area -->
      <div class="span4">
        <h3>Welcome!</h3>
        <p>This is <%= link_to "armory", "http://github.com/gorrillamcd/armory", :target => "_blank" %>, a web application for deploying Bible Colleges! It's still under heavy developement, but feel free to look around. If you find an error or have a suggestion, post it to the <%= link_to "issue tracker", "http://github.com/gorrillamcd/armory/issues", :target => "_blank" %>.</p>
          <hr />
        <p>Here's what's next on the TODO list:<br />
          <ul>
            <li>Complete course and lesson with all necessary fields/logic</li>
            <li>Add internationalization for translating to other languages</li>
            <li>Add user authentication(with devise) and authorization(with cancan)</li>
            <li>Create Exam model/logic/views along with Questions and Answers</li>
            <li>Catch up with writing tests for various features and switch to writing test <strong>first thing</strong></li>
            <li>Add validations and attr_accessor to all models</li>
          </ul>
        </p>
      </div>
      <!-- END Right Sidebar Area -->
      <% end %>
    </div>
</div>
</body>
</html>
